// named export
// exporting modules using names
export const a = 1;   
export const b = 2;

// default export
// we can name it anything while importing
let obj;
export default obj = {
    x:5,
    y:7
}